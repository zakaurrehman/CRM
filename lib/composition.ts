/**
 * The words under every composition table.
 *
 * The legend is the source tables' own convention. The disclaimer is IMS's
 * (14 September 2026), and it goes under every table on the site — the
 * material pages, the reference pages and the comparison — because the
 * tables are for identifying material, and a buyer must not read them as a
 * specification.
 */
export const compositionLegend = "* indicates maximum.";

export const compositionDisclaimer =
  "Chemical compositions are provided as typical or nominal reference values for material identification only. They do not constitute a purchasing or supply specification. Applicable standards, chemistry ranges and customer requirements must be confirmed before any transaction.";
